import { FilterMandatoryMissionPipe } from './filter-mandatory-mission.pipe';

describe('FilterMandatoryMissionPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterMandatoryMissionPipe();
    expect(pipe).toBeTruthy();
  });
});
